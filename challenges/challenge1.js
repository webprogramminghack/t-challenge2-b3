'use strict';

// Don't delete the code below
function setEventReminder(eventJSON) {
  // Convert JSON string to JavaScript object
  // Get the event date and current date
  // If the event date is in the future, schedule a reminder (using setTimeout)
  // Otherwise, indicate that the date has passed
}

// If you want to try the challenge in terminal, uncomment the code below
// const eventJSON = `{
//   "name": "Tech Conference",
//   "date": "2023-12-15",
//   "reminderTime": 5000 // 5 seconds
// }`;

// setEventReminder(eventJSON);

// Don't delete the code below
module.exports = setEventReminder;
